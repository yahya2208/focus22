/**
 * Gallery feature toggle.
 * Set USE_NEW_GALLERY to `true` to switch to the new scroll-snap gallery.
 * The legacy ProductImageGallery remains available for comparison.
 */
export const USE_NEW_GALLERY = true;
